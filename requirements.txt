# ══════════════════════════════════════════════════════════════════════════════
# NumaSec - Production Dependencies
# ══════════════════════════════════════════════════════════════════════════════
# NOTE: This file is DEPRECATED. Use pyproject.toml as source of truth.
# Kept for backward compatibility and container builds only.
#
# Install from pyproject.toml:
#   pip install -e .
# ══════════════════════════════════════════════════════════════════════════════

# Core - LLM APIs
openai>=1.30.0
anthropic>=0.25.0

# CLI Framework
rich>=13.7.0
prompt_toolkit>=3.0.43
typer>=0.12.0

# MCP Protocol
mcp>=1.0.0

# HTTP Client
httpx>=0.27.0

# HTML Parsing
beautifulsoup4>=4.12.0

# Vector DB for Knowledge Base
lancedb>=0.6.0
pyarrow>=14.0.0

# Embeddings & Search
sentence-transformers>=2.7.0
rank-bm25>=0.2.2

# ML for Meta-Learning Strategy
scikit-learn>=1.4.0
numpy>=1.26.0

# Database
sqlalchemy[asyncio]>=2.0.29
aiosqlite>=0.20.0

# Data Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Utilities
pyyaml>=6.0.0
aiofiles>=23.2.0

# Report Generation
python-docx>=1.1.0

# XML Parsing (for nmap output)
defusedxml>=0.7.1

# DNS queries (for recon_dns)
dnspython>=2.6.0

# ══════════════════════════════════════════════════════════════════════════════
# REMOVED: Unused dependencies (cleaned 2026-02-03)
# ══════════════════════════════════════════════════════════════════════════════
# python-dotenv - Not used (env vars loaded via os.environ)
# requests - Not used (replaced by httpx)
# Pillow - Not used (no image processing in current version)
# pyzbar - Not used (no QR code scanning)
# psutil - Not used (no process monitoring)
# pycryptodome - Not used (no crypto operations)
# watchdog - Not used (no file watching)
# playwright - Not used (HTTP-only, no browser automation)
# structlog - Not used (using standard logging)
# ══════════════════════════════════════════════════════════════════════════════
