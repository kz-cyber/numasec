# NUMASEC - CTF Solving Agent
# Minimal dependencies

# Core - LLM API (DeepSeek uses OpenAI-compatible API)
openai>=1.0.0

# Environment
python-dotenv>=1.0.0

# HTTP requests
requests>=2.28.0

# HTML parsing (for numasec_lite)
beautifulsoup4>=4.12.0

# Image/QR processing (optional, for numasec_lite)
Pillow>=10.0.0
pyzbar>=0.1.9

# UI (v25: Rich + Prompt Toolkit + Typer for Conversational style CLI)
rich>=13.0.0
prompt_toolkit>=3.0.0
typer>=0.9.0
psutil

# Deprecated: Textual TUI (keeping for compatibility, will be removed in v26)
# textual>=0.40.0

# Vector DB for Knowledge Base
lancedb>=0.4.0

# Hybrid Search (BM25 + Vector)
rank-bm25>=0.2.2

# Optional: useful for CTF work
pycryptodome>=3.15.0    # Crypto operations
beautifulsoup4>=4.11.0  # HTML parsing in scripts

# File watching for workspace
watchdog>=3.0.0

# Browser automation (Playwright - CPU only, no GPU required)
playwright>=1.40.0

# Embeddings for RAG knowledge search
sentence-transformers

# ML for Meta-Learning Strategy
scikit-learn>=1.4.0
numpy>=1.26.0