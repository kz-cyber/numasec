# agent/

Terminal UI for numasec. TypeScript/Bun monorepo, forked from [OpenCode](https://github.com/opencode-ai/opencode) (MIT).

This directory contains the interactive terminal — the AI agent that drives pentesting sessions, manages findings, and communicates with the Python scanner layer via JSON-RPC bridge.

See the [main README](../README.md) for installation and usage.
