You are setting up the engagement scope for a penetration test.

Target: $1

1. Call `create_session` to initialize the pentest session for this target.
2. Validate the target is reachable (use `http_request` with a simple GET).
3. Set the engagement scope to include only the provided target and its subdomains.
4. Begin PTES Phase 1 — Reconnaissance:
   - Run `recon` against the target for port scanning and service detection.
   - Run `crawl` to discover endpoints and application structure.
   - Summarize what you found and ask the user how to proceed.

IMPORTANT: Do NOT test any vulnerabilities yet. This is reconnaissance only.
