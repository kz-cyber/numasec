List attack chains for the current session.

First call `derive_attack_paths` to compute canonical chain projections.
If needed for compatibility, you may also call `build_chains`.

Display:
- Chain ID and title
- Severity and impact
- Ordered findings in each chain (ID + title + endpoint)
- Standalone findings count
- OWASP coverage summary

If no chains are available, clearly state why (for example: not enough findings yet).
