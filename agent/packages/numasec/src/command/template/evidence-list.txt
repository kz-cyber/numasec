List evidence-linked findings from the current session.

Call `get_findings` to retrieve findings, then show:
- Finding ID
- Severity
- Title
- URL / endpoint
- Evidence availability (request/response, payload, parameter)

Sort by severity (Critical > High > Medium > Low > Info).
If no findings exist yet, explain that evidence is not available yet.
