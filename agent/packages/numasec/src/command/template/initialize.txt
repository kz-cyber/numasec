Please analyze this target application and create an AGENTS.md file containing:

1. **Target Architecture & Technology Stack**
   - Web framework, language, runtime, database, cache, message queues
   - API style (REST, GraphQL, gRPC, WebSocket)
   - Frontend framework and rendering mode (SSR, SPA, static)
   - Infrastructure indicators (Docker, Kubernetes, cloud provider)

2. **Attack Surface Summary**
   - All entry points: HTTP routes, API endpoints, WebSocket handlers
   - File upload endpoints and storage mechanisms
   - Admin panels, debug endpoints, health checks
   - Third-party integrations and webhook receivers
   - Static assets and client-side JavaScript

3. **Authentication & Authorization Mechanisms**
   - Auth type: session-based, JWT, OAuth, API keys, mTLS
   - Session management: storage, expiry, rotation
   - Role-based access control (RBAC) or attribute-based (ABAC) patterns
   - Password hashing algorithms in use
   - Multi-factor authentication presence

4. **Security Configuration & Observations**
   - CORS policy (permissive or restrictive)
   - CSP headers, HSTS, X-Frame-Options
   - Rate limiting and throttling mechanisms
   - Input validation patterns (server-side, client-side, or both)
   - Error handling (verbose stack traces vs. generic messages)
   - Logging and monitoring (what gets logged, PII exposure risk)
   - Dependency versions and known CVE indicators

The file you create will be given to security assessment agents that operate against this target. Make it about 150 lines long.

If there's already an AGENTS.md, improve it if it's located in ${path}

$ARGUMENTS
