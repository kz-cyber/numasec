Show evidence details for finding "$ARGUMENTS".

Call `get_findings` to retrieve all findings from the current session, then locate the finding matching the given ID or title substring.

Display the full evidence for the matched finding:
- Finding title and severity
- Affected URL / endpoint
- HTTP request that triggered the finding (method, headers, body)
- HTTP response excerpt (status code, relevant headers, body snippet)
- Payload used (if any)
- Parameter tested
- CWE ID, CVSS score, OWASP category
- Remediation guidance

If no argument is provided, list all findings with their IDs so the user can pick one.
