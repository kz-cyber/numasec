Show all discovered credentials from the current pentest session.

Call `get_findings` to retrieve findings, then filter for any that contain credentials (look for findings with rule_id containing "auth", "credential", "password", or findings from auth_test).

Also call `relay_credentials` with action "list" to show any explicitly stored credentials.

Display results in a table:
- Username
- Password/Token (masked: show first 2 and last 2 chars)
- Source (where discovered)
- URL
- Valid? (confirmed working)

⚠️ SECURITY: Never display full credentials in reports. Always mask sensitive values.
