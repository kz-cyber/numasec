Store discovered credentials for use in subsequent authenticated tests.

Parameters:
- session_id (required): Session ID
- credentials (required): JSON object with credential data — {"username": "...", "password": "...", "type": "basic|bearer|cookie|api_key", "source": "where found"}

Returns confirmation of stored credentials (password is masked in output).
